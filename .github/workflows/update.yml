name: Auto Update News

on:
  schedule:
    # 06:00 CEST -> uruchom o 05:15 CEST (03:15 UTC)
    # 19:00 CEST -> uruchom o 18:15 CEST (16:15 UTC)
    - cron: '15 3,16 * * *'
  workflow_dispatch:


jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python update_news.py

      - name: Commit and push changes
        run: |
          git config --global user.name 'Bot Wiadomości'
          git config --global user.email 'bot@users.noreply.github.com'
          git add news.json archive.json
          git commit -m "Automatyczna aktualizacja newsów [skip ci]" || exit 0
          git push
